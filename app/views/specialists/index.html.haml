%h3
  = t(:specialists)
  %span.pull-right
    = link_to "#{t(:add_new)} #{t(:specialist)}", new_specialist_path, class: "btn btn-primary"
.listing
  - if @specialists.blank?
    %span.empty-message
      = t(:no_specialists)
  - else
    = paginate @specialists
    %table.table.table-condensed.table-responsive
      %thead
        %th= t(:name)
        %th= t(:specialty)
        %th= t(:practice_name)
        %th= t(:phone)
        %th= t(:email)
        %th

      %tbody
        - @specialists.each do |specialist|
          %tr
            %td= "#{specialist.first_name} #{specialist.last_name}"
            %td= specialist.specialty
            %td= specialist.practice_name
            %td= specialist.phone
            %td= specialist.email
            %td= link_to I18n.t(:edit_specialist), edit_specialist_path(specialist), class: 'btn btn-primary'
    = paginate @specialists

