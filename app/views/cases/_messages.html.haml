.messages
  - @case.messages.order("created_at desc")[0...@case.messages.count-1].each do |message|
    - if message.is_a? InfoMessage
      = render "info_message", message: message
    - else
      = render "message", message: message

